# FlashFill — راهنمای کامل نصب و استفاده

> **افزونه‌ای برای Anki که فلشکارت‌های زبان‌آموزی را به‌صورت خودکار پر می‌کند.**  
> فقط کلمه را تایپ کنید و دکمه Auto Fill را بزنید — ترجمه، تلفظ، مثال، صدا و تصویر همه خودکار پر می‌شوند.

---

## فهرست مطالب

1. [این افزونه چه کاری می‌کند؟](#این-افزونه-چه-کاری-می‌کند)
2. [نصب](#نصب)
3. [اولین اجرا — ساخت خودکار Note Type](#اولین-اجرا--ساخت-خودکار-note-type)
4. [تنظیمات پایه](#تنظیمات-پایه)
5. [دریافت کلید API](#دریافت-کلید-api)
   - [Google Gemini (رایگان)](#google-gemini-رایگان)
   - [OpenRouter (رایگان + مدل‌های متعدد)](#openrouter-رایگان--مدل‌های-متعدد)
   - [تصویر: Unsplash (رایگان)](#تصویر-unsplash-رایگان)
   - [تصویر: Pexels (رایگان)](#تصویر-pexels-رایگان)
6. [نحوه استفاده](#نحوه-استفاده)
7. [قابلیت صدا (تلفظ خودکار)](#قابلیت-صدا-تلفظ-خودکار)
8. [قابلیت تصویر (عکس خودکار)](#قابلیت-تصویر-عکس-خودکار)
9. [پنجره Preview](#پنجره-preview)
10. [Cache جلسه](#cache-جلسه)
11. [نگاشت فیلدها (Field Mapping)](#نگاشت-فیلدها-field-mapping)
12. [رفع مشکلات](#رفع-مشکلات)

---

## این افزونه چه کاری می‌کند؟

شما فقط یک کلمه یا عبارت می‌نویسید (مثلاً `bonjour`) و دکمه **✨ Auto Fill** را می‌زنید. افزونه به‌صورت خودکار این اطلاعات را پر می‌کند:

| فیلد | مثال |
|------|------|
| Translation | سلام / صبح به خیر |
| English | Hello / Good morning |
| Pronunciation | /bɔ̃.ʒuʁ/ |
| Part of Speech | Interjection |
| Gender | — |
| Example | Bonjour, comment ça va ? |
| Example Translation | سلام، حالت چطور است؟ |
| CEFR | A1 |
| Notes | رایج‌ترین سلام فرانسوی |
| Audio | 🔊 فایل صوتی تلفظ |
| Image | 🖼 عکس مرتبط |

برای **هر ترکیب زبانی** کار می‌کند: اسپانیایی→فارسی، ژاپنی→عربی، آلمانی→انگلیسی، و...

---

## نصب

### روش اول — کپی دستی پوشه

۱. برنامه **Anki** را باز کنید  
۲. از منو: **Tools → Add-ons → Open Add-ons Folder**  
۳. در پنجره‌ای که باز می‌شود، یک پوشه جدید بسازید، مثلاً:  
   `addons21\language_auto_fill\`  
۴. **تمام فایل‌های این پروژه** را داخل آن پوشه کپی کنید  
۵. Anki را ببندید و دوباره باز کنید  

### ساختار صحیح پوشه

```
addons21\
  └── language_auto_fill\
        ├── __init__.py
        ├── config.json
        ├── core\
        ├── providers\
        ├── ui\
        └── utils\
```

> ⚠️ مطمئن شوید `__init__.py` مستقیماً داخل `language_auto_fill\` باشد، نه یک سطح پایین‌تر.

---

## اولین اجرا — ساخت خودکار Note Type

دفعه اول که Anki را باز می‌کنید، افزونه به‌صورت **خودکار** یک Note Type به نام **"FlashFill"** می‌سازد که شامل همه ۱۲ فیلد لازم و یک قالب کارت زیبا است.

یک پیام تأیید می‌بینید. بعد از آن:

۱. در Anki دکمه **Add** را بزنید (یا کلید `A`)  
۲. در بالای صفحه، روی انتخاب‌گر Note Type کلیک کنید (پیش‌فرض "Basic" است)  
۳. **"FlashFill"** را انتخاب کنید  
۴. آماده‌اید!  

> **نیازی به تنظیم دستی فیلدها نیست.** همه چیز خودکار ساخته شده است.

---

## تنظیمات پایه

به **Tools → FlashFill: Settings…** بروید.

### تب General (عمومی)

| تنظیم | توضیح |
|-------|-------|
| Source Language | زبانی که **یاد می‌گیرید** (مثل Spanish، Japanese، French) |
| Target Language | زبانی که **ترجمه می‌خواهید** (مثل Persian، English) |
| Word input field | نام فیلدی که کلمه را می‌نویسید (پیش‌فرض: `Front`) |
| Show preview before applying | پنجره‌ای برای بررسی قبل از ذخیره نشان می‌دهد |
| Session Cache | تعداد کلمات کش‌شده را نشان می‌دهد + دکمه پاک‌کردن |

### تب Provider (ارائه‌دهنده AI)

| تنظیم | توضیح |
|-------|-------|
| Provider | `mock` (تست بدون اینترنت)، `gemini`، یا `openrouter` |
| API Key | کلید API ارائه‌دهنده انتخابی |
| Model | فقط برای OpenRouter — مدل AI (مثل `google/gemma-3-27b-it:free`) |

### تب Audio (صدا)

| تنظیم | توضیح |
|-------|-------|
| Enable audio | فعال/غیرفعال کردن دانلود صدا |
| TTS Provider | `mock` (فایل ساکت برای تست) یا `gtts` (Google TTS رایگان) |
| Audio field name | نام فیلد صدا در Note Type (باید دقیقاً با آن تطبیق داشته باشد) |

### تب Image (تصویر)

| تنظیم | توضیح |
|-------|-------|
| Enable image | فعال/غیرفعال کردن دانلود تصویر |
| Image Provider | `mock` (عکس آبی برای تست)، `unsplash`، یا `pexels` |
| API Key | برای Unsplash یا Pexels لازم است |
| Image field name | نام فیلد تصویر (باید دقیقاً با آن تطبیق داشته باشد) |

### تب Field Mapping (نگاشت فیلدها)

هر نوع داده را به فیلد صحیح در Note Type وصل کنید.  
اگر خالی بگذارید، آن نوع داده نادیده گرفته می‌شود.

---

## دریافت کلید API

### Google Gemini (رایگان)

Gemini یک **تیر رایگان سخاوتمندانه** دارد: ۱۵ درخواست در دقیقه، یک میلیون token در روز.

**مراحل دریافت کلید:**

۱. به آدرس [https://aistudio.google.com/app/apikey](https://aistudio.google.com/app/apikey) بروید  
۲. با حساب Google وارد شوید  
۳. روی **"Create API Key"** کلیک کنید  
۴. کلید را کپی کنید (با `AIza...` شروع می‌شود)  

**تنظیم در Anki:**

۱. **Tools → FlashFill: Settings… → تب Provider**  
۲. Provider را روی `gemini` بگذارید  
۳. کلید را در فیلد API Key جای‌گذاری کنید  
۴. Save کنید  

> ✅ **برای مبتدیان توصیه می‌شود.** نیاز به کارت بانکی ندارد.

---

### OpenRouter (رایگان + مدل‌های متعدد)

OpenRouter به بیش از **۱۰۰ مدل AI** (GPT-4، Claude، Llama، Gemma و...) از طریق یک کلید API دسترسی می‌دهد. بسیاری از مدل‌ها **کاملاً رایگان** هستند.

#### مرحله ۱ — ساخت حساب

۱. به [https://openrouter.ai](https://openrouter.ai) بروید  
۲. روی **Sign In** کلیک کنید  
۳. با حساب GitHub یا Google وارد شوید (یا ایمیل ثبت کنید)  

#### مرحله ۲ — دریافت کلید API

۱. به [https://openrouter.ai/keys](https://openrouter.ai/keys) بروید  
۲. روی **Create Key** کلیک کنید  
۳. یک نام بدهید (مثلاً: `Anki Auto Fill`)  
۴. کلید را کپی کنید (با `sk-or-...` شروع می‌شود)  

#### مرحله ۳ — تنظیم در Anki

۱. **Tools → FlashFill: Settings… → تب Provider**  
۲. Provider را روی `openrouter` بگذارید  
۳. کلید را جای‌گذاری کنید  
۴. مدل را انتخاب کنید  
۵. Save کنید  

#### مدل‌های رایگان پیشنهادی

| مدل | ID برای وارد کردن | توضیح |
|-----|-----------------|-------|
| Google Gemma 3 27B | `google/gemma-3-27b-it:free` | ✅ پیش‌فرض، کیفیت عالی |
| Meta Llama 3.3 70B | `meta-llama/llama-3.3-70b-instruct:free` | بسیار قوی، رایگان |
| DeepSeek R1 | `deepseek/deepseek-r1-0528:free` | استدلال قوی |
| Mistral 7B | `mistralai/mistral-7b-instruct:free` | سریع و سبک |

برای دیدن همه مدل‌های رایگان: [https://openrouter.ai/models?order=top-weekly](https://openrouter.ai/models?order=top-weekly) → فیلتر **Free**

> 💡 اگر مدل رایگان پر شده باشد، یک اعتبار کوچک ($۱-۵) به حساب اضافه کنید تا به مدل‌های با کیفیت‌تر دسترسی داشته باشید.

---

### تصویر: Unsplash (رایگان)

**۵۰ درخواست در ساعت — رایگان**

۱. به [https://unsplash.com/developers](https://unsplash.com/developers) بروید  
۲. روی **Register as a developer** کلیک کنید و حساب رایگان بسازید  
۳. روی **New Application** کلیک کنید  
۴. شرایط را قبول کنید، نام و توضیح بنویسید  
۵. پایین صفحه بخش **"Keys"** را ببینید — **Access Key** را کپی کنید  

**تنظیم در Anki:**  
Settings → **تب Image** → Provider: `unsplash` → API Key: کلید Access Key را جای‌گذاری کنید

---

### تصویر: Pexels (رایگان)

**۲۰۰ درخواست در ساعت — رایگان**

۱. به [https://www.pexels.com/api/](https://www.pexels.com/api/) بروید  
۲. روی **Get Started** کلیک کنید و حساب رایگان بسازید  
۳. به [https://www.pexels.com/api/new/](https://www.pexels.com/api/new/) بروید  
۴. فرم را پر کنید و ارسال کنید  
۵. کلید API در صفحه نشان داده می‌شود — کپی کنید  

**تنظیم در Anki:**  
Settings → **تب Image** → Provider: `pexels` → API Key: کلید را جای‌گذاری کنید

---

## نحوه استفاده

### گام به گام

۱. **پنجره Add را باز کنید** (کلید `A` در Anki)  
۲. Note Type را **"FlashFill"** انتخاب کنید  
۳. کلمه یا عبارت را در فیلد **Front** بنویسید  
   مثال‌ها: `auf Wiedersehen`، `おはよう`، `merci beaucoup`، `hola`  
۴. روی **✨ Auto Fill** در نوار ابزار کلیک کنید  
   (یا کلید میانبر **Ctrl+Shift+A**)  
۵. یک نوار پیشرفت ظاهر می‌شود  
۶. **پنجره Preview** باز می‌شود — همه داده‌ها را بررسی کنید  
۷. روی **✓ Apply to Note** کلیک کنید تا ذخیره شود  
   یا **✕ Cancel** برای انصراف  

### نکات مهم

- **فیلدهای پر هرگز بازنویسی نمی‌شوند** — فقط فیلدهای خالی پر می‌شوند
- اگر فیلدی در Note Type وجود نداشته باشد، بدون خطا نادیده گرفته می‌شود
- UI هنگام دریافت داده freeze نمی‌کند — می‌توانید کارهای دیگر انجام دهید

---

## قابلیت صدا (تلفظ خودکار)

افزونه از **Google Translate TTS** (رایگان، بدون API Key) برای دانلود فایل MP3 تلفظ استفاده می‌کند.

**تنظیم:**  
۱. Settings → **تب Audio**  
۲. تیک "Enable automatic audio pronunciation" را بزنید  
۳. TTS Provider را روی `gtts` بگذارید  
۴. مطمئن شوید Audio field name با نام فیلد در Note Type مطابقت دارد  

**نحوه کار:**  
- فایل MP3 در پوشه Anki Media ذخیره می‌شود  
- فیلد Audio با `[sound:autofill_es_mucho_gusto.mp3]` پر می‌شود  
- در مرور کارت، صدا خودکار پخش می‌شود  

**زبان‌های پشتیبانی‌شده:** اسپانیایی، فرانسوی، آلمانی، ژاپنی، عربی، فارسی، ترکی، چینی، کره‌ای، روسی، و بیش از ۲۰ زبان دیگر.

---

## قابلیت تصویر (عکس خودکار)

افزونه بر اساس **ترجمه انگلیسی کلمه** یک عکس مرتبط جستجو می‌کند.

**هوش مصنوعی Query:**
- اسم: `perro` + english="dog" → جستجو: `"dog"`
- فعل: `comer` + english="to eat" → جستجو: `"person eating"`
- عبارت: `mucho gusto` + english="Nice to meet you" → جستجو: `"Nice to meet you two people"`

**تنظیم:**  
Settings → **تب Image** → provider و API Key را تنظیم کنید

---

## پنجره Preview

بعد از دریافت داده، پنجره Preview همه اطلاعات را قبل از ذخیره نشان می‌دهد:

- همه فیلدهای زبانی با مقادیر آنها
- تصویر دانلودشده (نمایش live)
- تگ فایل صوتی

**دکمه‌ها:**
- **✓ Apply to Note** — داده‌ها را در کارت بنویس و ببند
- **✕ Cancel** — همه چیز را دور بینداز، کارت تغییر نکند
- **🔄 Regenerate Image** — بدون بستن پنجره، عکس جدیدی دریافت کن

---

## Cache جلسه

افزونه نتایج را در طول جلسه Anki کش می‌کند. اگر همان کلمه را دوباره Auto Fill کنید، نتیجه کش‌شده فوراً برمی‌گردد (بدون API call جدید).

- کش با بستن Anki پاک می‌شود
- پاک‌کردن دستی: Settings → General → **🗑 Clear Cache**

---

## نگاشت فیلدها (Field Mapping)

اگر از **Note Type سفارشی** خودتان استفاده می‌کنید (نه "FlashFill"):

۱. Settings → **تب Field Mapping**  
۲. برای هر نوع داده، نام دقیق فیلد در Note Type خود را بنویسید  
۳. برای رد کردن هر نوع داده، فیلد آن را خالی بگذارید  

---

## رفع مشکلات

| مشکل | راه‌حل |
|------|--------|
| دکمه Auto Fill دیده نمی‌شود | Anki را کاملاً ببندید و دوباره باز کنید |
| خطای "Trigger field not found" | تنظیم "Word input field" در Settings باید دقیقاً با نام فیلد در Note Type مطابقت داشته باشد |
| داده‌ای برنمی‌گردد (mock mode) | به `gemini` یا `openrouter` با API Key معتبر تغییر دهید |
| کلید API رد شد (خطای 401) | کلید را دوباره کپی کنید — مطمئن شوید فاصله اضافی ندارد |
| تصویر در کارت نشان داده نمی‌شود | فیلد `Image` باید در قالب کارت Anki وجود داشته باشد |
| صدا پخش نمی‌شود | مطمئن شوید `Audio` در قالب کارت هست و فایل در Media وجود دارد |
| خطای rate limit (429) | سهمیه رایگان تمام شده. یک دقیقه صبر کنید یا پلن را ارتقا دهید |
| خطای "no credits" در OpenRouter | برای مدل‌های پولی، اعتبار اضافه کنید ($۱-۵) یا از مدل رایگان استفاده کنید |
| Note Type "FlashFill" ساخته نشد | Anki را ببندید و دوباره باز کنید. اگر همچنان نساخت، پوشه add-on را چک کنید |

---

## تست اولیه (بدون API Key)

می‌توانید افزونه را **کاملاً بدون API Key** تست کنید:

۱. Settings → **تب Provider**: Provider = `mock`  
۲. Settings → **تب Audio**: Provider = `mock`  
۳. Settings → **تب Image**: Provider = `mock`  
۴. یک کلمه در Front بنویسید و Auto Fill بزنید  
۵. داده‌های placeholder ظاهر می‌شوند  

وقتی مطمئن شدید همه چیز درست کار می‌کند، Provider را به `gemini` یا `openrouter` تغییر دهید.

---

*ساخته‌شده با ❤️ برای زبان‌آموزان*
